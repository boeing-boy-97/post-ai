# Allow esbuild's postinstall to run on Vercel/build hosts so the native binary is installed.
# Suppresses: "esbuild@0.21.5 (postinstall: node install.js)" allow-scripts warning.
enable-pre-post-scripts=true
